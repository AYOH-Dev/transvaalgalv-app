# Frontend (Transvaal Galv)

Minimal React + Vite frontend scaffold.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` to `http://10.100.0.1:8083` for local dev if needed.
